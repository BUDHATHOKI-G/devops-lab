pipeline {
    agent any

    stages {
        stage('Checkout Verification') {
            steps {
                echo 'Repository successfully checked out from GitHub!'
            }
        }
    }
}
